"""``aida chat`` — interactive streaming chat with a provider.

Placeholder: implemented in Phase 2 (provider layer + agent loop). Present
here only so the repository structure in PLAN.md §3 exists from Phase 1.
"""

from __future__ import annotations


def main(_argv: list[str] | None = None) -> int:
    print("aida chat: not yet implemented (arrives in Phase 2 — see PLAN.md §10).")
    return 1
