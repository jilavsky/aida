"""``aida run`` — headless execution of stored named workflows.

Placeholder: implemented in Phase 10. Present here only so the repository
structure in PLAN.md §3 exists from Phase 1.
"""

from __future__ import annotations


def main(_argv: list[str] | None = None) -> int:
    print("aida run: not yet implemented (arrives in Phase 10 — see PLAN.md §10).")
    return 1
