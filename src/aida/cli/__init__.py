"""AIDA command-line interface.

Phase 1 ships only ``aida doctor``. ``chat.py`` (Phase 2), ``run.py``
(Phase 10), and ``config_cmds.py`` are placeholders that later phases fill
in — see PLAN.md §10.
"""
